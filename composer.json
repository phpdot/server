{
    "name": "phpdot/server",
    "description": "Swoole HTTP, WebSocket, and TCP server for PSR-15 handlers.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Omar Hamdan",
            "email": "omar@phpdot.com"
        }
    ],
    "keywords": [
        "server",
        "swoole",
        "http",
        "websocket",
        "tcp",
        "psr-15"
    ],
    "require": {
        "ext-swoole": ">=6.2",
        "php": ">=8.5",
        "composer-runtime-api": "^2.2",
        "phpdot/console": "^0.2",
        "phpdot/contracts": "^0.2",
        "phpdot/http": "^0.2",
        "psr/container": "^2.0",
        "psr/http-factory": "^1.0",
        "psr/http-message": "^2.0",
        "psr/http-server-handler": "^1.0",
        "symfony/console": "^8.0"
    },
    "require-dev": {
        "phpdot/redis": "^0.2",
        "friendsofphp/php-cs-fixer": "^3.94",
        "phpdot/container": "^0.2",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^13.0",
        "swoole/ide-helper": "^6.0"
    },
    "suggest": {
        "ext-pcntl": "Forwards SIGINT/SIGTERM from server:start to the master for a clean stop (guarded at runtime).",
        "ext-posix": "Lets the cluster heartbeat report the true master pid (guarded at runtime).",
        "phpdot/container": "Autowires the container-attribute services and #[Config] DTOs declared in src (the attributes stay inert until reflected, so standalone consumers don't need it installed).",
        "phpdot/redis": "Publishes this node into the Redis cluster registry via the class_exists-guarded Heartbeat."
    },
    "autoload": {
        "psr-4": {
            "PHPdot\\Server\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPdot\\Server\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=256M",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "check": [
            "@test",
            "@analyse",
            "@cs-check"
        ]
    },
    "config": {
        "sort-packages": true
    },
    "support": {
        "source": "https://github.com/phpdot/server",
        "issues": "https://github.com/phpdot/monorepo/issues"
    }
}
